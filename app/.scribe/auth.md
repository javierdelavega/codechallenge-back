# Authenticating requests

To authenticate requests, include an **`Authorization`** header with the value **`"Bearer N7fp6GTjO9CJD1QIhqv0Ty1ZZbJeS3tFIbToFJZQ"`**.

All authenticated endpoints are marked with a `requires authentication` badge in the documentation below.

Can retrieve a token by issuing a GET Request to /api/accesstoken
